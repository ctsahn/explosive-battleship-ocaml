(library
  (name board)
  (modules board)
  (libraries core))

(executable
 (name server)
 (modules server template game)
 
 (libraries dream core core_unix board)
 (preprocess (pps lwt_ppx)))
 

 (rule
 (targets template.ml)
 (deps template.eml.ml)
 (action
  (run dream_eml %{deps} --workspace %{workspace_root})))
